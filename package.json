{
  "name": "motionjs",
  "version": "1.0.0",
  "description": "Lightweight 3D multiplayer game engine",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel dev",
    "build": "pnpm --parallel build",
    "lint": "prettier --check .",
    "format": "prettier --write .",
    "typecheck": "pnpm --parallel typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3",
    "prettier": "^3.1.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
